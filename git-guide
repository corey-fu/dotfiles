## Basic ## 


# Clone git repo

git clone https://github.com/USER/REPO.git



# Add  a single file or dir to repo

git add file.txt

# Add all files or dirs to repo

git add --all

# removing files from the staging area

git reset HEAD <file>




# Commit a message for all files

git commit -m "Hello World!"

# Commit a message for single file

git commit -m "master" path/to/my/file.txt

# removing files from the staging area and the tree
# (How to remove stuffs in Git)

git rm --cached <file-name>

# if a dir

git rm -r --cached <dir>

# Nuke all made changes for good

git reset --hard




# Push on origin:master

git push origin master 






## Change repo name ##

Go to github & change repo name (click repo->settings)

# Change local repo name (etc. dotfiles)

mv .dotfiles dotfiles

# Set the new url (origin)

git remote set-url origin https://github.com/USER/REPO.git
